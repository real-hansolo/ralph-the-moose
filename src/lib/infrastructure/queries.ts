// TODO: Implement checkCorrectNetworks
import { DEFAULT_CHAIN } from "./config/chains"

const checkCorrectNetwork = () => {
    // const defaultNetwork = DEFAULT_CHAIN
    // const currentNetwork = "eth"
    // if (defaultNetwork.chainId !== currentNetwork.chainId) {
    //     throw new Error(`Please connect to ${defaultNetwork.name} network`)
    // }
}